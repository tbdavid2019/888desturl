version: "3.9"

services:
  redirect-tracer:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    shm_size: "1gb"
    restart: unless-stopped
